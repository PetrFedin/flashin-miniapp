{
  "name": "@flashin/platform-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test dist/*.test.js",
    "check": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
